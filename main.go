package main

import "fmt"

func main() {
	// cards := newDeck()

	// // cards.print()
	// hold, change := cards.deal(2)

	// hold.print()
	// change.print()

	greeting := "Hi there!"
	// parse of string to byte slice
	fmt.Println([]byte(greeting))
}